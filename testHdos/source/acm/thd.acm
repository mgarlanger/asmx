$THD	SPACE	4,10
**	$THD	-  TYPE HEX DIGITS
*
*	$THD TYPES A BYTE AS 2 HEX DIGITS, ZERO FILL.
*
*	ENTRY:	(A)	= VALUE
*	EXIT:	VALUE TYPED
*	USES:	(PSW)
*

$THD	PUSH	PSW
	ANI	0F0H		MAP OUT LOW ORDER NIBBLE
	RRC
	RRC
	RRC
	RRC			(A) = (A) % 16
	CALL	THD.
	CALL	$TYPC.		TYPE OUT THE FIRST DIGIT
	POP	PSW

	ANI	00FH		MAP OUT THE HIGH ORDER NIBBLE
	CALL	THD.
	JMP	$TYPC.		TYPE OUT THE SECOND DIGIT

THD.	CPI	10
	JNC	THD1		(A) >= 10

	ADI	'0'
	RET

THD1	ADI	'A'-10
	RET
