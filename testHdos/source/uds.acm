	SPACE	4,10
**	UDS - UPDATE DIRECTORY SECTOR.
*
*	UDS UPDATES THE DIRECTORY SECTOR BACK ONTO THE DISK.
*
*	ENTRY	SECSCR = SECTOR
*	EXIT	'C' CLEAR IF OK
*		'C' SET IF ERROR
*		 (A) = CODE
*	USES	ALL

UDS	LXI	B,512
	LHLD	S.SCR
	XCHG				; DE = SECTOR SCRATCH
	LXI	H,DIS.SEC
	DAD	D
	CALL	$HLIHL			; HL = THIS SECTOR NUMBER
	JMP	DWRITE			; WRITE AND EXIT
