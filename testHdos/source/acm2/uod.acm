$UOD	SPACE	4,10
**	$UOD - UNPACK OCTAL DIGITS.
*
*	$UOD CONVERTS A SINGLE BYTE INTO 3 OCTAL DIGITS., ZERO FILL
*
*	ENTRY	(A) = BYTE VALUE
*		(H,L) = ADDRESS OF 3 BYTE AREA FOR DIGITS
*	EXIT	(H,L) = (H,L)+3
*	USES	A,H,L


$UOD	PUSH	B
	MVI	B,3		(B) = LOOP COUNT
	ANA	A		CLEAR CARRY

UOD1	RAL
	RAL
	RAL
	PUSH	PSW		SAVE VALUE
	ANI	7
	ADI	'0'
	MOV	M,A		STORE DIGIT
	INX	H
	POP	PSW		RESTORE VALUE
	DCR	B
	JNZ	UOD1		IF MORE TO GO
	POP	B		RESTORE (B,C)
	RET			   EXIT
